---
name: interactive-tutorial-labs
description: Add input-driven interactive teaching labs to Claude Code mechanism tutorials by reusing the repository runtime, preserving evidence accuracy, and parallelizing one scenario per worker.
---

# Interactive Tutorial Labs

Before implementation, read and follow:

- `../../../skill/interactive-tutorial-labs/SKILL.md`

Load its supporting references only when routed by that document.

The visible file under `skill/interactive-tutorial-labs/` is the canonical source. Do not duplicate or independently expand these instructions in `.claude/skills/`.
